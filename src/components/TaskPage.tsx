import { Box, Container, Typography } from "@mui/material";

export const TaskPage = () => {
  return (
    <Container>
      <Box>
        <Typography>Name of Task</Typography>
        <Typography>Starting at: price of task</Typography>
      </Box>
    </Container>
  );
};
